# LangGraph 및 LangChain
langgraph>=0.2.0
langchain>=0.3.0
langchain-openai>=0.2.0
langsmith>=0.1.0  # LangSmith 추적 (선택사항)

# OpenAI
openai>=1.0.0

# 임베딩 & Reranking
FlagEmbedding>=1.2.0

# 벡터 DB
chromadb>=0.5.0

# 데이터 처리
pandas>=2.0.0
pyarrow>=14.0.0
numpy>=1.24.0

# 설정 파일
pyyaml>=6.0.0

# 웹 검색 (선택사항)
tavily-python>=0.3.0

# 로깅 & 유틸리티
tqdm>=4.65.0
python-dotenv>=1.0.0  # .env 파일 로드 (선택사항)

# 시각화 (선택사항)
graphviz>=0.20.0  # 그래프 시각화
